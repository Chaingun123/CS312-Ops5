apiVersion: v1
kind: Service
metadata:
  name: minecraft-metrics
  namespace: default        
  labels:
    app: minecraft
spec:
  selector:
    app: minecraft          
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
